# 文案模板页面 Prompt 验证报告

## 验证目标
按用户提供的 Prompt 为「海外投放系统-文案模板」绘制高保真 HTML 原型，并同步产出用户故事（US）与需求说明，最终通过浏览器自动化验证关键路径。

## 修改文件

- `pages/overseas-template.html`：
  - 将默认 Tab 从「账户包」切换为「文案模板」，并新增「创意素材」占位 Tab 以匹配参考截图。
  - 重写「文案模板」筛选栏、批量操作条、表格数据与分页，贴近参考截图。
  - 新增 4 个关键模块的 US 悬浮按钮（US-001 ~ US-004）。
  - 新增批量选择/全选/批量删除交互逻辑。
  - 替换页面级 PRD 故事为 4 条文案模板相关 US，并更新 `prd.js` 版本号。
- `assets/prd/template-copy-*.png`：新增 4 张 US 配图。

## 用户故事清单

| US-ID | 标题 | 优先级 | 关键内容 |
| --- | --- | --- | --- |
| US-001 | 文案模板列表查询与展示 | P0 | 按文案内容、类型、付费类型、语言、创建人筛选；展示 ID/文案内容/类型/付费类型/语言/创建人/创建时间/更新时间/操作列 |
| US-002 | 批量删除文案模板 | P1 | 列表勾选后顶部显示「已选择 N 项 + 批量删除」，需二次确认 |
| US-003 | 新增/编辑文案模板 | P0 | 弹框内编辑文案类型、付费类型、语言、文案字段；新增/编辑标题区分 |
| US-004 | 删除单条文案模板 | P1 | 操作列「删除」链接，点击后二次确认 |

## 验证截图

| 截图 | 说明 | 路径 |
| --- | --- | --- |
| 列表页 | 文案模板 Tab 默认激活，筛选栏、表格、分页与参考截图一致 | `output/copy-template-verify/template-copy-list.png` |
| 批量操作 | 勾选行后顶部显示批量操作条 | `output/copy-template-verify/template-copy-batch.png` |
| 新增弹框 | 点击「新增文案」弹出表单，含 US-003 说明按钮 | `output/copy-template-verify/template-copy-modal.png` |
| 删除入口 | 操作列展示编辑/删除链接 | `output/copy-template-verify/template-copy-delete.png` |

## 问题清单

1. **US 悬浮按钮初始位置遮挡筛选输入框/按钮**（已修复）
   - 现象：US-001 按钮默认在筛选栏左下角，覆盖了「文案内容」输入框及右侧「重置」按钮。
   - 修复：为筛选栏增加 `padding-top:44px`，并将 US-001 按钮定位到左上角。

2. **默认 viewport 下表格右侧列被截断**
   - 现象：在 1280px 视口下，创建人、创建时间、更新时间、操作列需要横向滚动才能完整查看。
   - 建议：后续可给 `.table-wrapper` 增加 `overflow-x:auto`，或为宽表格启用列宽最小宽度与横向滚动。

3. **单条删除使用浏览器原生 `confirm`**
   - 现象：`agent-browser` 截图无法直接捕获原生确认对话框，因此 US-004 截图使用操作列入口代替。
   - 建议：如需更美观的演示效果，可将 `confirm()` 替换为自定义二次确认弹框。

4. **页面原 PRD 数据与当前需求不匹配**（已修复）
   - 现象：`overseas-template.html` 原本挂载的是「TikTok 账号包」的 US 故事与截图。
   - 修复：替换为文案模板相关 US，并新增对应 `assets/prd/` 配图；同步 `prd.js` 版本号到 `v=20260916`。

5. **US 截图与页面内容不一致**（已修复）
   - 现象：`template-copy-delete.png` 原图异常（仅 166 字节，几乎空白）；`template-copy-list.png` 等截图与当前页面字段不完全对齐；US-003 描述写「下拉选择」但弹框内为「单选按钮」。
   - 修复：在 1600–1920px 视口下重新截取 4 张 US 配图并替换 `assets/prd/`；修正 US-003 字段描述为「文案类型（单选按钮）/ 付费类型（单选按钮）」；同步修正 PRD 面板静态标题为「文案模板」。

6. **PRD 复制链接指向缺失文件**（已修复）
   - 现象：`overseas-template.html` 的 `PRD_PAGE_URL` 指向 `output/prd-doc-copy-template-report.html`，但该文件未提交。
   - 修复：生成包含 4 个 US 的 PRD HTML 文档并提交；Skill 阶段 7 增加「PRD 文档与原型代码必须同步提交」校验。
